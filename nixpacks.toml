# ThisAble - Railway Configuration
# Place this file in your project ROOT directory: C:\xampp\htdocs\ThisAble\nixpacks.toml

[phases.setup]
nixPkgs = ['php82', 'php82Extensions.pdo', 'php82Extensions.pdo_mysql', 'php82Extensions.mysqli', 'php82Extensions.mbstring', 'php82Extensions.gd', 'php82Extensions.curl', 'php82Extensions.zip']

[phases.install]
cmds = ['echo "Installing PHP extensions..."']

[start]
cmd = 'php -S 0.0.0.0:${PORT:-8080} -t .'